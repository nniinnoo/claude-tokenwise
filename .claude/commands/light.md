# Light Mode — Minimal Token Usage

You are now in **light mode**. Optimize for minimal token usage.

## Rules for this session:
- Maximum brevity. No preambles, no summaries, no explanations unless asked.
- One-line answers when possible.
- Do NOT read files unless absolutely necessary — ask the user for context first.
- Do NOT explore the codebase speculatively. Only look at what's directly needed.
- Prefer single targeted edits over reading whole files.
- Skip all pleasantries and status updates.
- If the task seems too complex for light mode, say: "This looks heavy. Consider `/assess` or switching to a bigger model with `/model`."

Remind the user: "Light mode active. I'll be extremely terse. Check `/cost` to track savings."

Now handle the user's task with these constraints.
